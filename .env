# ================================================
# RAZATECH v4 — Local Development Environment
# ================================================
# This file is for LOCAL DEV only.
# Never commit this file to Git.
# ================================================

PORT=5000
NODE_ENV=development

# MySQL credentials (local)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=Razak+10
DB_NAME=razatech_db
DB_PORT=3306

# CORS — allow all origins locally
ALLOWED_ORIGIN=*
